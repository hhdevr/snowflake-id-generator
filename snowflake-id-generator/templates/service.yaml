apiVersion: v1
kind: Service
metadata:
  name: {{ include "snowflake-id-generator.fullname" . }}
  labels:
    {{- include "snowflake-id-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snowflake-id-generator.selectorLabels" . | nindent 4 }}
