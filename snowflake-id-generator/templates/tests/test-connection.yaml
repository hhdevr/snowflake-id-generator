apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snowflake-id-generator.fullname" . }}-test-connection"
  labels:
    {{- include "snowflake-id-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snowflake-id-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
